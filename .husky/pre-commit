#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Check for ESLint errors in staged files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx|js|jsx)$')

if [ -n "$STAGED_FILES" ]; then
    echo "📋 Checking ESLint on staged files..."
    
    # Check frontend files
    FRONTEND_FILES=$(echo "$STAGED_FILES" | grep "^frontend/" || true)
    if [ -n "$FRONTEND_FILES" ]; then
        cd frontend
        npx eslint $FRONTEND_FILES --max-warnings 10
        if [ $? -ne 0 ]; then
            echo "❌ ESLint errors found in frontend. Please fix them before committing."
            exit 1
        fi
        cd ..
    fi
    
    # Check backend files
    BACKEND_FILES=$(echo "$STAGED_FILES" | grep "^backend/" || true)
    if [ -n "$BACKEND_FILES" ]; then
        cd backend
        npx eslint $BACKEND_FILES
        if [ $? -ne 0 ]; then
            echo "❌ ESLint errors found in backend. Please fix them before committing."
            exit 1
        fi
        cd ..
    fi
fi

echo "✅ Pre-commit checks passed!"